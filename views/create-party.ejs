<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a New Party</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <h2>Create a New Party</h2>
        <% if (error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/voter/create-party" method="POST" enctype="multipart/form-data">
            <label for="partyname">Party Name:</label>
            <input type="text" id="partyname" name="name" required>

            <label for="manifesto">Manifesto:</label>
            <textarea id="manifesto" name="manifesto" rows="4" required></textarea>

            <label for="image">Party Logo:</label>
            <input type="file" id="image" name="image" accept="image/*">

            <button type="submit">Create Party</button>
        </form>
    </div>
</body>
</html>
